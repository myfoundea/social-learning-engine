  <% if object.errors.any? %>
    <div id="error_explanation" class="border-red-400 p-4 rounded border bg-red-100 mb-6">
      <h2 class="text-sm heading text-red-500"><%= pluralize(object.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul class="text-sm mt-2 text-red-500 pl-4">
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>